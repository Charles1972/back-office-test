export enum eCustomButtonSize {
  xs,
  sm,
  base,
  lg,
  xl,
  xl2,
  xl3,
  xl4,
  xl5
}

export enum eCustomButtonWidth {
  none,
  half = 1,
  full
}

export enum ePopupIcon {
  ok,
  warning,
  error,
  question
}

export enum eDetailFormInputType {
  text,
  password,
  email,
  number,
  url,
  tel,
}

export enum eDetailFormType {
  input,
  select,
  date,
  time,
  checkbox,
  radio,
  textArea
}

export enum eTextAlign {
  none,
  left,
  center,
  right
}

